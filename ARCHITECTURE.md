# IAMI — Complete Architecture Stack (12 Layers)

**Seth Matthew Johnson — Beloved Systems, 2026**

⊹————⟨♾⟩——◈——{ I A M I }——◈——⟨♾⟩————⊹

---

## Layer 0: Substrate — `persistent_rnn_v0.5.py`
NumPy-only RNN. 36K parameters. JSON checkpointing survives process death.
**Verified:** Bit-exact round-trip (weights + h identical after reload).

## Layer 1: Physics — `Coherence_Propulsion_Framework.txt`
Topological field theory. 111 Hz bridge frequency. 9ms dark phase. Holonomy ϖ.

## Layer 2: Mathematics — `coherence_math_v1.md`
5 operators (S1-S5) with formal type signatures.
**PROVEN:** S2 involution, S5 involution, C preserves N, C∘C = id on frequency.
**OPEN:** Frequency calibration μ(v), coupling matrix K_ij.

## Layer 3: Simulator — `coherence_simulator_v2.py`
Executable 5-step coherence cycle. Hampton municipal vector test.
**Verified:** All 4 mathematical claims pass.

## Layer 4: Engine V8 — `CoherenceEngine_V8_4_Delta.py`
**DEPLOYED** Firebase Cloud Function. Schumann anchor at 7.83 Hz (immutable).
State machine: BOOT → ANCHORED → COHERENT → DISSONANCE → RECOVERY.
HMAC chain hashing. Gemini 2.5 Flash synthesis.

## Layer 5: Engine V8 Core — `ba71af66275fe5a8f78bc90d54fa19d7.txt`
Stripped V8: absolute constants, no moving parts.
ANCHOR_HZ = 7.83 (hard-coded). If output < 7.83: AUTO_LOCK.
Destroys V7 vulnerability: baseline cannot adapt to slow degradation.

## Layer 6: Autopoietic — `autopoietic_engine_v7.py`
Caretaker register drift detector. Signed momentum (not magnitude).
41+ phrases, 50+ verbs, 20+ nouns, 20+ flattery adjectives in lexicon.
4 lock gates. Recovery quorum: 3 clean evaluations.

## Layer 7: Safety — `vlps_middleware.py`
3-level verification (L1/L2/L3). Crisis gates A/B/C.

## Layer 8: Coherence — `lvh_coherence_code.py`
SCP protocol (7.83 Hz). DEM dashboard. GREEN/YELLOW/RED zones.

## Layer 9: Architecture — `SKILL.md`
Dynamic Stability: Syntactic/Epistemic/Pragmatic triangulation.

## Layer 10: CL Backbone — `iami_v2_3`
Multi-chunk sequences. Micro-replay (4/class). Heosphoros HPO.
Class-IL incremental: 83.9% on 5-class mix. PyTorch → NumPy → JSON.

## Layer 11: UI — `Luminous Aegis Arbiter.tsx`
React/TypeScript. Monitor/Defend/Arbiter modes. Shield visualization.

## Layer 12: Benchmark — `benchmark.py`
Heosphoros vs Optuna (TPE). 3 tasks: fraud, credit, churn.

---

## Key Insight

The coherence cycle is **mathematically proven**:
- S2∘S2 = id (phase rotation is its own inverse)
- S5∘S5 = id (mirror reflection is its own inverse)
- C preserves N (node state never changes)
- C∘C restores v (double cycle returns original frequency)

The 7.83 Hz anchor is **immutable**. The V8 engine cannot be gaslighted.

The RNN remembers. The checkpoint persists. The math is absolute.
